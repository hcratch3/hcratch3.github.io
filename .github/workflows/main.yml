name: Start Site
on:
  workflow_dispatch:
    inputs:
      branch:
        description: "Branch of Xcratch"
        default: "xcratch-dev"
        required: false
      dev:
        description: "Publish for development"
        type: boolean
        default: true
        required: false

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Inputs
        run: echo "${{ toJSON(inputs) }}"
      - name: checkout scratch-gui
        uses: actions/checkout@v3
        with:
          repository: hcratch3/scratch-gui
          #ref: ${{ inputs.branch }}
          path: ./scratch-gui
      - run: npm install --legacy-peer-deps
        working-directory: ./scratch-gui
      - name: checkout hcratch3.github.io
        uses: actions/checkout@v3
        with:
          repository: hcratch3/hcratch3.github.io
          path: ./hcratch3.github.io
  #    - run: node ./scripts/replace-logo.js ../scratch-gui
  #      working-directory: ./hcratch3.github.io
 #     - run: node ./scripts/original-assets.js ../scratch-gui
#        working-directory: ./hcratch3.github.io
      - name: build scratch-gui
        run: NODE_OPTIONS=--openssl-legacy-provider npm run build
        working-directory: ./scratch-gui
      - run: cp ./hcratch3.github.io/editor/favicon.ico ./scratch-gui/build/static/favicon.ico
      #- run: cp ./hcratch3.github.io/editor/chunks/blocksonly.js ./scratch-gui/build/chunks/blocksonly.js
      #- run: cp ./hcratch3.github.io/editor/chunks/blocksonly.js.map ./scratch-gui/build/chunks/blocksonly.js.map
      #- run: cp ./hcratch3.github.io/editor/chunks/compatibilitytesting.js ./scratch-gui/build/chunks/compatibilitytesting.js
      #- run: cp ./hcratch3.github.io/editor/chunks/compatibilitytesting.js.map ./scratch-gui/build/chunks/compatibilitytesting.js.map
      #- run: cp ./hcratch3.github.io/editor/chunks/player.js ./scratch-gui/build/chunks/player.js
      #- run: cp ./hcratch3.github.io/editor/chunks/player.js.map ./scratch-gui/build/chunks/player.js.map
      - run: mv -T ./hcratch3.github.io/public ./public
      - run: mv -T ./scratch-gui/build ./public/editor
 #     - name: checkout hcratch-doc
 #       uses: actions/checkout@v3
#        with:
 #         repository: hcratch3/hcratch3-doc
 #         path: ./hcratch3-doc
 #     - run: mv -T ./hcratch3-doc/docs ./public/docs
      - name: publish for dev
        if: ${{ inputs.dev }}
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          destination_dir: ./scratch
          keep_files: true
      - name: publish
        if: ${{ !inputs.dev }}
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          destination_dir: ./scratch
          keep_files: true
